{
  "name": "middleware",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "dev:test": "cross-env NODE_ENV=test nodemon index.js",
    "lint": "eslint .",
    "build:ui": "rm -rf build && cd ../part3-3 && npm run build && cp -r build ../part3-5",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push -u origin main",
    "test": "cross-env NODE_ENV=test jest --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.3.0"
  },
  "devDependencies": {
    "eslint": "^8.43.0",
    "eslint-plugin-react": "^7.32.2",
    "jest": "^29.5.0",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "globalTeardown": "./tests/teardown.js"
  }
}
